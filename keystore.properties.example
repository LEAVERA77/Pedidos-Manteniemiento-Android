# Opcional / respaldo: la firma release del módulo app está definida en app/build.gradle.kts
# (ruta absoluta C:/Keystore/keystore). Este archivo no es leído por Gradle para firmar.
#
# Si en el futuro volvés a centralizar secretos fuera del repo, usá un mecanismo que no los suba a Git.

storeFilePath=C:/Keystore/keystore
storePassword=
keyAlias=
keyPassword=
